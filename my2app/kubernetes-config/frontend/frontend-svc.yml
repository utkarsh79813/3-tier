apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  labels:
    class: frontend
spec:
  selector:
    class: frontend
  type: NodePort
  ports:
  - name: frontend-ports
    port: 5000
    targetPort: 3000
    nodePort: 32000